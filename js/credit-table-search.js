function tableSearch16() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-16");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-16");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch17() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-17");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-17");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch18() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-18");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-18");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch19() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-19");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-19");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch20() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-20");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-20");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch21() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-21");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-21");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch22() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-22");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-22");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}

function tableSearch23() {
    var input, filter, table, tr, td, td2, i, txtValue;
    input = document.getElementById("table-input-23");
    filter = input.value.toUpperCase();
    table = document.getElementById("c-l-t-23");
    tr = table.getElementsByTagName("tr");
    for (i = 1; i < tr.length; i++) {
        td = tr[i].getElementsByTagName("td")[1];
        if (td || td2) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "table-row",
                i++;
                tr[i].style.display = "table-row";
            } else {
                // tr[i+1].style.display = "table-row";
                tr[i].style.display = "none";
                
            }
        }
    }
}